import 'package:flutter/material.dart';

class PedidosEnviadosPage extends StatelessWidget {
  const PedidosEnviadosPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
          body: Container(
       child: Center(child: Text("Pedidos Pendientes")),
      ),
    );
  }
}